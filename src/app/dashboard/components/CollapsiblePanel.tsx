'use client';

// Re-exported from the shared location so every app can use the same collapse
// mechanism. Keep this file so existing dashboard/trends/guest imports keep working.
export { default } from '@/components/ui/CollapsibleSection';
